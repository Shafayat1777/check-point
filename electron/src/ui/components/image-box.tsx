import { Image } from 'lucide-react';

const ImageBox = () => {
    return (
        <div>
            <div className="bg-muted flex h-40 w-40 items-center justify-center">
                <Image />
            </div>
        </div>
    );
};

export default ImageBox;
